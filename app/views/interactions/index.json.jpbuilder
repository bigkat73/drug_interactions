json.interactions @interaction.interactions do |interaction|
    json.medications interaction[:medications] do |med|
        json.id med[:id]
        json.name med[:name]
    end
    json.severity interaction[:severity]
end